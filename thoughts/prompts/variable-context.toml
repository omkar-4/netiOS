[task]
objective = "{EXPLICIT_CORE_TASK}"
structs = "{REQUIRED_STRUCTS_AND_MEMORY_ALIGNMENT}"
api = "{REQUIRED_FUNCTIONS}"

[verification]
success = "{EXPECTED_WORKING_STATE_OR_SERIAL_OUTPUT}"
failure = "{EXPECTED_HARDWARE_CRASH_IF_INCORRECT}"
