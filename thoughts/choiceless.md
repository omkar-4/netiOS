## Hardware-enforced Unavoidable Choiceless Components
- GDT (global descriptor table)
- IDT (interrupt descriptor table)
- APIC (advanced programmable interrupt controller)
- Page Tables (virtual memory management)
- Physical Frame Allocator


Awesome. I need you to recursively outline the implementation details independent of implementation language in an ASCII (using only '|' and '-' symbols for tree structure)  character code multi-level tree-format for each one of them starting with the core component's name at top level down to its atomic indivisible element unit.

start with outlining the GDT.
reply in plain text format.


This is confusing I would like to go deeper into this async mechanism once we have the outlines for the rest choiceless hardware-enforced elements.

- GDT e
- IDT (interrupt descriptor table)
- APIC (advanced programmable interrupt controller)
- Page Tables (virtual memory management)
- Physical Frame Allocator

next for these one by one.
Now, for IDT I need you to recursively outline the implementation details independent of implementation language in an ASCII character code (using only '|' and '-' symbols for tree structure) multi-level tree-format for each one of them starting with the core component's name at top level down to its atomic indivisible element unit.

start outlining the IDT.
reply in plain text format.