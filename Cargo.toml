[package]
name = "netiOS"
version = "0.1.0"
edition = "2024"

[dependencies]
limine = "0.5.0"
noto-sans-mono-bitmap = {
    version = "0.3",
    features = ["size_24"]
}

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
